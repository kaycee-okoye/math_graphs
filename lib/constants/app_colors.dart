import 'package:flutter/material.dart';

import 'graph_colors.dart';

/// Color scheme used in graph design
class AppColors {
  static const transparent = Colors.transparent;
  static const graphBackground = Color.fromARGB(255, 250, 250, 250);
  static const graphTitle = Color.fromARGB(255, 255, 0, 0);
  static const graphGrid = graphTitle;
  static const List<Color> graphColors = GRAPH_COLORS;
}
